/* eslint-disable react/prop-types */
import React from 'react';
// import FiveRandNum from '../Utils/FiveRandNum';

export default function CardPicker(props) {
  const [cards] = props;
  // const picked = FiveRandNum();
  // const mapped = picked.map((num) => cards[num]);
  // eslint-disable-next-line no-console
  console.log(cards);
  return (
    <div>
      poo
    </div>
  );
}
